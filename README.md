type "python chess.py --train" to start training, or type "python chess.py" to play against the trained AI for windows 
type "python3 chess.py --train" to start training, or type "python3 chess.py" to play against the trained AI for Mac OS
